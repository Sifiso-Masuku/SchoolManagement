@model IEnumerable<SchoolManagement.Model.Entity.ClassFee>

@{
    ViewBag.Title = "Index";
    //Layout = "~/Views/Shared/_HomePageLayout.cshtml";
}

<h2>Class Fees</h2>

<p>
    @Html.ActionLink("Add New Class Fee ", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassName.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FeeType.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassName.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FeeType.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
